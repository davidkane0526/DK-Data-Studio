'use strict';
self.DKDSTaskDefinition=Object.freeze({async run(input){const A=self.DKDSScience;if(!A?.analyzePulseReadData)throw new Error('Pulse task science runtime unavailable.');const file=input?.file||{},options={...(input?.options||{})};if(options.segmentationMode==='auto'&&Number(options.cycleSamples)<=1){const estimate=A.estimatePulseCycleSamples?.(input?.inspection,{currentCol:options.currentCol,voltageCol:options.voltageCol})||0;if(Number(estimate)>1){options.cycleSamples=Math.round(Number(estimate));options.__autoEstimatedCycle=true;}}try{return A.analyzePulseReadData(file,options);}catch(firstError){if(options.segmentationMode==='auto'&&options.__autoEstimatedCycle===true){const fallback={...options,cycleSamples:0};delete fallback.__autoEstimatedCycle;try{return A.analyzePulseReadData(file,fallback);}catch{} }throw firstError;}}});
