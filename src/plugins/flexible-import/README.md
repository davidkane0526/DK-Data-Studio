# Flexible Text Import

Plugin id: `builtin.flexible-import`

Generic text/multicolumn import provider used by the import workbench.

This built-in plugin uses the shared `DKDSScience` engine. The preserved `main` v3.14 implementation is used only by parity/migration tests, not as the runtime scientific engine.
