# Generated tool blueprint parity reference

This is the second independent production-shaped Phase F reconstruction sample.

It uses the accepted Pulse Sampler blueprint only as external structural evidence. The generator itself has no Pulse id or branch. The declarative source proves that the public SDK can express the generic tool-designer vocabulary that was not exercised by the first Vth sample:

- Tool host + utility PRIMARY;
- bounded Workspace leftWidth / leftMin / leftReserve and content end inset;
- titleless embedded parameter PRIME with explicit priority and auto-open policy;
- compact Tabs;
- form-grid-2 parameter fields;
- action-grid-4 actions;
- ordinary Toolbar projected through segment-bar layout;
- parameter-owned Table;
- ScientificPlot and main result Table.

No production Pulse state, waveform algorithm, Task, CSS, DOM hooks or plugin-specific Presenter logic is copied.
