# Resonance Unit-only Shadow Reconstruction

Parallel SDK 1.51.3 / Unit Templates 2.5.3 reconstruction of the frozen native `builtin.resonance-workbench`.

It intentionally does **not** call `ctx.ui.scientificWorkbench` or the `accepted-scientific-v1` whole-workbench preset. The accepted scientific PRIMARY, data-control PRIME, inspector PRIME and PlotGroup PRIME are manually composed from the same public Unit Template facade that the preset itself uses.

Production Resonance source and authored CSS remain byte-frozen. This example is seven-layer parity evidence only and does not duplicate the private stateful Resonance domain service.
