# Scientific parity baseline

This fixture preserves the scientific engine from DK Data Studio v3.61.58 (`c3c159c`).
It is intentionally stored as test data so parity validation is deterministic and does not depend on Git branches, repository history, or network access.

Update this fixture only through an explicit scientific-baseline review; normal release checkpoints must not silently move it.
