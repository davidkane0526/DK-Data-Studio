{
  "sdkVersion": "1.10.0",
  "pluginApiVersion": "1.10.0",
  "packageSchema": 1,
  "minimumAppVersion": "3.60.0",
  "manifestSchema": "plugin-manifest.schema.json",
  "entryGlobal": "DKDSPlugins",
  "notes": [
    "New plugins target Plugin API 1.10.0.",
    "Plugin API 1.10 adds ctx.data.reactive for scientific transactions, dependency propagation and stale async-result rejection.",
    "The SDK is standalone and does not require DK Data Studio source code.",
    "Domain logic belongs to plugins; application infrastructure is consumed through ctx.* Core APIs."
  ]
}
